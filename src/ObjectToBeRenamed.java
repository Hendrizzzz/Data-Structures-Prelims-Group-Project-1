/**
 * Represents the object to be sorted.
 */
public class ObjectToBeRenamed implements Comparable<ObjectToBeRenamed> {
    // this is the object to be sorted

    @Override
    public int compareTo(ObjectToBeRenamed o) {
        return 0;
    }
}
